## Multi Git Account Access
Don't forget to `git init` in the ~/personal folder in order to initialize the
local git config for that folder. If you don't do that, cloning will not work
with the personal SSH key.
